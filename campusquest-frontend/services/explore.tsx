// app/(tabs)/explore.tsx
// Archivo fallback requerido por Expo Router cuando existen
// variantes por plataforma (.native.tsx / .web.tsx).
// En la práctica nunca se carga directamente.
export { default } from "./explore.web"